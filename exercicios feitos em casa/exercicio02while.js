// Loop de 1 a 15
for (let i = 1; i <= 15; i++) {
    // Verifica se o número é ímpar
    if (i % 2 !== 0) {
        // Exibe o número ímpar
        document.write(i + "<br>"); // Ou use document.write(i + "<br>") se quiser mostrar na página
    }
}
